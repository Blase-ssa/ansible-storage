---
#########################################################################
#временно размещю эти переменные тут, потом перетащу
prog_release:	"{{ kafka_prog_release }}"
prog_scale_num:	"{{ kafka_prog_scale_num }}"
prog_mirror:	"{% mirror_list[ range(0, mirror_list|length) | random ] %}"

#########################################################################

role_name: 'kafka'
role_service_accaunt: kafka

# path to store programm
programm_path: /opt/kafka

# download url
download_url: "{{ prog_mirror }}/{{ role_name }}/{{ prog_release }}/{{ role_name }}_{{ prog_scale_num }}-{{ prog_release }}.tgz"
...
